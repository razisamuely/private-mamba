# Status Report — 2026-04-10

**Branch**: `feat/swimmer-action-fix`

## Done
- `SwimmerWrapper.py` — discrete→continuous action mapping (9 → 2D), obs/cost pipeline wired
- Horizon scaling fix in `DreamerLearner.py` — `scaled_limit = cost_limit * (horizon / max_steps)` so λ actually reacts
- `train.py` — `MAX_STEPS` config, WandB name truncation, debug mode
- Metric alignment in `DreamerWorker.py` — uses `total_episode_reward` for safety gym envs
- Logic verified locally via `verify_scaling.py`

## Where We Stopped
- Local pilot hit OOM (32GB RAM exhausted)
- Cluster submission to `slurm.bgu.ac.il` timed out
- Branch not yet pushed to remote

## Next Steps (in order)
1. Push branch to remote cluster
2. Run **A** (short) — verify score/cost sanity after action fix
3. Run **B** (~50k steps) — verify score + cost + λ together
4. Tune `laglr` only if λ is still broken after B
5. Clean up `train.py` init logic + verify sbatch generation
